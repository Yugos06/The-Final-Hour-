---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - descriptor.mod
  - common/national_focus/fh_france_focus.txt
  - common/national_focus/france.txt
  - common/national_focus/FRA_focus.txt
  - common/ideas/fh_france_ideas.txt
  - common/ideas/FRA_ideas.txt
  - common/decisions/fh_france_decisions.txt
  - common/decisions/categories/fh_france_decision_categories.txt
  - common/on_actions/fh_on_actions.txt
  - common/on_actions/FRA_on_actions.txt
  - events/fh_france_events.txt
  - events/FRA_events.txt
autonomous: true
requirements:
  - FOUND-01
user_setup:
  - service: paradox-launcher
    why: "Paradox enregistre une copie de descriptor.mod hors du repo qui doit rester synchrone"
    dashboard_config:
      - task: "Mettre à jour supported_version vers 1.18.* dans le fichier de registration Paradox"
        location: "/home/yugos06/.local/share/Paradox Interactive/Hearts of Iron IV/mod/The Final Hour.mod"

must_haves:
  truths:
    - "Le mod cible HoI4 1.18.* dans descriptor.mod et le fichier Paradox external"
    - "Tous les fichiers avec préfixe fh_fra_ et fh_on_actions ont été supprimés du repo"
    - "Un fichier shadow common/national_focus/france.txt existe pour écraser l'arbre vanilla french_focus"
    - "Les squelettes FRA_focus.txt, FRA_ideas.txt, FRA_on_actions.txt et FRA_events.txt existent et sont syntaxiquement valides"
    - "Le focus_tree FRA_focus_tree a un score factor=0 + modifier add=10 pour FRA"
  artifacts:
    - path: "descriptor.mod"
      provides: "Déclaration mod ciblant HoI4 1.18.*"
      contains: 'supported_version="1.18.*"'
    - path: "common/national_focus/france.txt"
      provides: "Shadow file vide pour écraser l'arbre vanilla french_focus"
      contains: "id = french_focus"
    - path: "common/national_focus/FRA_focus.txt"
      provides: "Squelette focus_tree FRA_focus_tree avec priorité +10 pour FRA"
      contains: "FRA_focus_tree"
    - path: "common/ideas/FRA_ideas.txt"
      provides: "Squelette ideas vide pour les esprits nationaux FRA"
      contains: "ideas = {"
    - path: "common/on_actions/FRA_on_actions.txt"
      provides: "Squelette on_actions pour les triggers FRA Phase 3"
      contains: "on_actions = {"
    - path: "events/FRA_events.txt"
      provides: "Namespace fh_france pour les events FRA Phase 3"
      contains: "add_namespace = fh_france"
  key_links:
    - from: "common/national_focus/france.txt"
      to: "vanilla french_focus tree"
      via: "Override par chemin relatif identique"
      pattern: "id = french_focus"
    - from: "common/national_focus/FRA_focus.txt"
      to: "FRA tag selection"
      via: "country block factor + modifier"
      pattern: "tag = FRA"
---

<objective>
Reconstruction propre de l'infrastructure de scripts mod : suppression complète de l'ancien code alpha avec préfixe fh_fra_ (D-04), création des squelettes minimalement valides avec le nouveau préfixe FRA_ (D-05), mise en place du shadow file vanilla pour écraser l'arbre français vanilla (FOUND-01), et correction de la version cible HoI4 dans le descriptor (1.14.* → 1.18.*).

Purpose: Pose la fondation script du mod. Après ce plan, HoI4 doit charger le mod sans erreur de syntaxe et sans focuses vanilla FRA résiduels. Toutes les phases suivantes s'appuient sur ces squelettes.

Output: 4 squelettes valides (focus, ideas, on_actions, events), 1 shadow file (france.txt), descriptor mis à jour, 5 fichiers alpha supprimés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Pattern vanilla extrait de Hearts of Iron IV/common/national_focus/france.txt -->
<!-- Doit être reproduit fidèlement dans common/national_focus/france.txt (shadow) -->

Vanilla focus_tree header:
```
focus_tree = {
  id = french_focus
  country = {
    factor = 0
    modifier = {
      add = 10
      tag = FRA
    }
  }
  default = no
}
```

Pattern descriptor.mod (vérifié contre installation 1.18.1):
```
name="The Final Hour"
version="0.1"
supported_version="1.18.*"
tags={
  "Alternate History"
  "National Focuses"
  "Events"
}
replace_path="common/bookmarks"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Supprimer le code alpha fh_fra_ et mettre à jour descriptor.mod</name>
  <files>descriptor.mod, common/national_focus/fh_france_focus.txt, common/ideas/fh_france_ideas.txt, common/decisions/fh_france_decisions.txt, common/decisions/categories/fh_france_decision_categories.txt, common/on_actions/fh_on_actions.txt, events/fh_france_events.txt</files>
  <read_first>
    - descriptor.mod (état actuel : supported_version="1.14.*")
    - common/national_focus/fh_france_focus.txt (à supprimer mais lire pour confirmer qu'aucun pattern non-documenté n'y traîne)
    - common/on_actions/fh_on_actions.txt (à supprimer — vérifier qu'il n'y a pas de déclarations utiles à reporter dans le squelette FRA_on_actions.txt)
    - .planning/phases/01-foundation/01-CONTEXT.md §decisions D-04 (liste exacte des fichiers à supprimer)
    - .planning/phases/01-foundation/01-RESEARCH.md §Piège 2 (mismatch version 1.14 vs 1.18.1 installé)
  </read_first>
  <action>
    Implémente D-04 (suppression complète du code alpha fh_fra_) et la correction de version (Piège 2 RESEARCH).

    1. Supprimer ces 5 fichiers exactement (commande `rm`, ne pas en oublier un seul) :
       - common/national_focus/fh_france_focus.txt
       - common/ideas/fh_france_ideas.txt
       - common/decisions/fh_france_decisions.txt
       - common/decisions/categories/fh_france_decision_categories.txt
       - common/on_actions/fh_on_actions.txt
       Note : events/fh_france_events.txt est ÉGALEMENT supprimé (D-04 implicite + Pattern Map indique qu'il sera remplacé par FRA_events.txt avec le même namespace fh_france).

    2. Réécrire descriptor.mod avec EXACTEMENT ce contenu (ligne `supported_version` passe de "1.14.*" à "1.18.*") :

```
name="The Final Hour"
version="0.1"
supported_version="1.18.*"
tags={
  "Alternate History"
  "National Focuses"
  "Events"
}
replace_path="common/bookmarks"
```

    3. NE PAS toucher au fichier Paradox externe `/home/yugos06/.local/share/Paradox Interactive/Hearts of Iron IV/mod/The Final Hour.mod` dans cette tâche — c'est traité par user_setup et signalé au joueur lors de l'exécution. Mentionner dans le SUMMARY que ce fichier doit être mis à jour manuellement OU re-régénéré par le launcher Paradox au prochain lancement.

    4. Encodage : tous les fichiers `.txt` et `.mod` en UTF-8 SANS BOM (vérifier avec hexdump après écriture). Pas de CRLF — utiliser LF (Linux).
  </action>
  <acceptance_criteria>
    - `test ! -f common/national_focus/fh_france_focus.txt && echo OK` retourne `OK`
    - `test ! -f common/ideas/fh_france_ideas.txt && echo OK` retourne `OK`
    - `test ! -f common/decisions/fh_france_decisions.txt && echo OK` retourne `OK`
    - `test ! -f common/decisions/categories/fh_france_decision_categories.txt && echo OK` retourne `OK`
    - `test ! -f common/on_actions/fh_on_actions.txt && echo OK` retourne `OK`
    - `test ! -f events/fh_france_events.txt && echo OK` retourne `OK`
    - `grep -c 'supported_version="1.18.\*"' descriptor.mod` retourne `1`
    - `grep -c 'supported_version="1.14.\*"' descriptor.mod` retourne `0`
    - `grep -c 'replace_path="common/bookmarks"' descriptor.mod` retourne `1`
    - `hexdump -C descriptor.mod | head -1` ne commence PAS par `ef bb bf` (pas de BOM)
  </acceptance_criteria>
  <verify>
    <automated>test ! -f common/national_focus/fh_france_focus.txt && test ! -f common/ideas/fh_france_ideas.txt && test ! -f common/decisions/fh_france_decisions.txt && test ! -f common/decisions/categories/fh_france_decision_categories.txt && test ! -f common/on_actions/fh_on_actions.txt && test ! -f events/fh_france_events.txt && grep -q 'supported_version="1.18.\*"' descriptor.mod && echo PASS</automated>
  </verify>
  <done>5 fichiers fh_*.txt + 1 events/fh_france_events.txt supprimés ; descriptor.mod cible 1.18.* sans BOM ; aucune référence à fh_fra_ ou fh_on_actions ne subsiste dans common/.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Créer le shadow file france.txt et le squelette FRA_focus.txt</name>
  <files>common/national_focus/france.txt, common/national_focus/FRA_focus.txt</files>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md (section "common/national_focus/france.txt" et "common/national_focus/FRA_focus.txt" — patterns exacts à recopier)
    - .planning/phases/01-foundation/01-RESEARCH.md §Piège 1 (nom de fichier shadow MUST be france.txt, NOT 00_france.txt)
    - .planning/phases/01-foundation/01-CONTEXT.md §decisions D-06 (stratégie factor=0 + modifier add=10)
  </read_first>
  <action>
    Implémente FOUND-01 (shadow file) + le squelette focus tree (D-05/D-06).

    1. Créer common/national_focus/france.txt avec EXACTEMENT ce contenu (le nom doit être `france.txt` — PAS `00_france.txt` selon Piège 1 RESEARCH) :

```
# common/national_focus/france.txt
# Shadow file — efface l'arbre vanilla France (french_focus)
# Ne PAS mettre de contenu ici : FRA_focus.txt porte notre arbre
# Source : pattern vanilla Hearts of Iron IV/common/national_focus/france.txt

focus_tree = {
  id = french_focus
  country = { factor = 0 }
  default = no
}
```

    2. Créer common/national_focus/FRA_focus.txt avec EXACTEMENT ce contenu (per D-05 — squelette ; per D-06 — factor=0 + modifier add=10 tag=FRA) :

```
# common/national_focus/FRA_focus.txt
# Phase 1 skeleton — sera complété en Phase 5
# Source : pattern vanilla france.txt + research Pattern 2

focus_tree = {
  id = FRA_focus_tree

  country = {
    factor = 0
    modifier = {
      add = 10
      tag = FRA
    }
  }

  default = no
  reset_on_civilwar = yes

  # PHASE 5 : focus = { id = FRA_lheure_la_plus_sombre ... }
}
```

    3. Encodage : UTF-8 sans BOM, line endings LF.

    4. Ne PAS ajouter de bloc `focus = { }` — le squelette vide est volontaire pour Phase 1. Phase 5 ajoutera tous les focuses.
  </action>
  <acceptance_criteria>
    - `test -f common/national_focus/france.txt && echo OK` retourne `OK`
    - `test -f common/national_focus/FRA_focus.txt && echo OK` retourne `OK`
    - `grep -c 'id = french_focus' common/national_focus/france.txt` retourne `1`
    - `grep -c 'id = FRA_focus_tree' common/national_focus/FRA_focus.txt` retourne `1`
    - `grep -c 'tag = FRA' common/national_focus/FRA_focus.txt` retourne `1`
    - `grep -c 'add = 10' common/national_focus/FRA_focus.txt` retourne `1`
    - `grep -c 'reset_on_civilwar = yes' common/national_focus/FRA_focus.txt` retourne `1`
    - `grep -c 'default = no' common/national_focus/france.txt` retourne `1`
    - `grep -c 'focus = {' common/national_focus/FRA_focus.txt` retourne `0` (squelette vide voulu)
    - `hexdump -C common/national_focus/france.txt | head -1` ne commence PAS par `ef bb bf`
    - `hexdump -C common/national_focus/FRA_focus.txt | head -1` ne commence PAS par `ef bb bf`
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'id = french_focus' common/national_focus/france.txt && grep -q 'id = FRA_focus_tree' common/national_focus/FRA_focus.txt && grep -q 'tag = FRA' common/national_focus/FRA_focus.txt && grep -q 'add = 10' common/national_focus/FRA_focus.txt && [ $(grep -c 'focus = {' common/national_focus/FRA_focus.txt) -eq 0 ] && echo PASS</automated>
  </verify>
  <done>france.txt shadow file existe avec id=french_focus ; FRA_focus.txt existe avec id=FRA_focus_tree, factor=0+modifier add=10 tag=FRA, reset_on_civilwar=yes, sans bloc focus= ; les deux fichiers en UTF-8 sans BOM.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Créer les squelettes FRA_ideas.txt, FRA_on_actions.txt et FRA_events.txt</name>
  <files>common/ideas/FRA_ideas.txt, common/on_actions/FRA_on_actions.txt, events/FRA_events.txt</files>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md (sections "FRA_ideas.txt", "FRA_on_actions.txt", "FRA_events.txt" — patterns exacts)
    - .planning/phases/01-foundation/01-CONTEXT.md §decisions D-05 (squelettes minimaux valides obligatoires)
    - .planning/phases/01-foundation/01-RESEARCH.md (section "Exemples de code" — squelettes validés)
  </read_first>
  <action>
    Implémente D-05 (squelettes minimalement valides pour ideas, on_actions, events).

    1. Créer common/ideas/FRA_ideas.txt avec EXACTEMENT ce contenu :

```
# common/ideas/FRA_ideas.txt
# Phase 1 skeleton — sera complété en Phase 2
# Source : pattern vanilla ideas.txt

ideas = {
  country = {
    # PHASE 2 : FRA_esprit_francais = { ... }
  }
}
```

    2. Créer common/on_actions/FRA_on_actions.txt avec EXACTEMENT ce contenu :

```
# common/on_actions/FRA_on_actions.txt
# Phase 1 skeleton — sera complété en Phase 3
# Source : pattern vanilla on_actions.txt

on_actions = {
  on_startup = {
    effect = {
      # PHASE 3 : FRA = { add_ideas = FRA_esprit_francais }
    }
  }
}
```

    3. Créer events/FRA_events.txt avec EXACTEMENT ce contenu (le namespace `fh_france` est CONSERVÉ — voir PATTERNS.md ligne "namespace reste fh_france (pas de renommage)") :

```
# events/FRA_events.txt
# Phase 1 skeleton — sera complété en Phase 3
# Source : pattern events/fh_france_events.txt

add_namespace = fh_france

# PHASE 3 : country_event = { id = fh_france.001 ... }
```

    4. Encodage : UTF-8 sans BOM, LF line endings.

    5. NE PAS ajouter de bloc `country_event = { }`, `add_ideas = ...`, ou `country = { ... }` non-vide — les squelettes sont volontairement vides. Toute référence à un ID inexistant casserait le chargement.
  </action>
  <acceptance_criteria>
    - `test -f common/ideas/FRA_ideas.txt && echo OK` retourne `OK`
    - `test -f common/on_actions/FRA_on_actions.txt && echo OK` retourne `OK`
    - `test -f events/FRA_events.txt && echo OK` retourne `OK`
    - `grep -c '^ideas = {' common/ideas/FRA_ideas.txt` retourne `1`
    - `grep -c '^on_actions = {' common/on_actions/FRA_on_actions.txt` retourne `1`
    - `grep -c 'on_startup = {' common/on_actions/FRA_on_actions.txt` retourne `1`
    - `grep -c '^add_namespace = fh_france' events/FRA_events.txt` retourne `1`
    - `grep -c 'country_event' events/FRA_events.txt` retourne `0` (Phase 3)
    - `grep -c 'fh_fra_' common/ideas/FRA_ideas.txt common/on_actions/FRA_on_actions.txt events/FRA_events.txt` retourne `0` (aucune référence à l'ancien préfixe)
    - `hexdump -C common/ideas/FRA_ideas.txt | head -1` ne commence PAS par `ef bb bf`
    - `hexdump -C common/on_actions/FRA_on_actions.txt | head -1` ne commence PAS par `ef bb bf`
    - `hexdump -C events/FRA_events.txt | head -1` ne commence PAS par `ef bb bf`
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^ideas = {' common/ideas/FRA_ideas.txt && grep -q '^on_actions = {' common/on_actions/FRA_on_actions.txt && grep -q 'on_startup = {' common/on_actions/FRA_on_actions.txt && grep -q '^add_namespace = fh_france' events/FRA_events.txt && [ $(grep -rc 'fh_fra_' common/ideas/FRA_ideas.txt common/on_actions/FRA_on_actions.txt events/FRA_events.txt | grep -v ':0' | wc -l) -eq 0 ] && echo PASS</automated>
  </verify>
  <done>FRA_ideas.txt, FRA_on_actions.txt et FRA_events.txt existent ; chacun a sa structure minimale valide (ideas/country, on_actions/on_startup/effect, add_namespace=fh_france) ; aucune référence à fh_fra_ ; UTF-8 sans BOM.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disque local → moteur Paradox | Le moteur HoI4 lit les fichiers script en lecture seule depuis le dossier mod local. Aucune entrée externe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | N/A | Fichiers script Clausewitz | accept | Domaine local, mod single-player, aucune surface réseau ni input utilisateur. ASVS non applicable conformément au contexte security_enforcement de la phase. |
</threat_model>

<verification>
Après les 3 tâches, lancer :
- `find . -name 'fh_fra_*.txt' -o -name 'fh_on_actions.txt' -o -name 'fh_france_*.txt' | wc -l` → doit retourner `0`
- `find common/national_focus/ common/ideas/ common/on_actions/ events/ -name 'FRA_*.txt' -o -name 'france.txt' | sort` doit lister exactement : `common/ideas/FRA_ideas.txt`, `common/national_focus/FRA_focus.txt`, `common/national_focus/france.txt`, `common/on_actions/FRA_on_actions.txt`, `events/FRA_events.txt`
- `grep -l 'supported_version="1.18.\*"' descriptor.mod` doit retourner `descriptor.mod`
- Test manuel HoI4 (utilisateur) : lancer le mod, vérifier que le launcher Paradox n'affiche plus l'avertissement "version obsolète"
</verification>

<success_criteria>
1. 6 fichiers alpha supprimés du repo (5 fh_*.txt + 1 events/fh_france_events.txt)
2. descriptor.mod cible HoI4 1.18.*
3. Shadow file `common/national_focus/france.txt` existe avec id=french_focus, écrasant l'arbre vanilla
4. Squelettes FRA_focus.txt (avec factor/modifier FRA), FRA_ideas.txt, FRA_on_actions.txt, FRA_events.txt existent et sont syntaxiquement minimaux mais valides
5. Aucune référence résiduelle à fh_fra_ ou fh_on_actions dans les fichiers script
6. Tous les fichiers sans BOM (encodage UTF-8 pur — vérifié avec hexdump)
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-foundation/01-01-SUMMARY.md` documentant :
- Fichiers supprimés (liste exacte)
- Fichiers créés (liste exacte avec rôle)
- Modifications descriptor.mod (avant/après supported_version)
- Note importante : l'utilisateur DOIT mettre à jour OU laisser le launcher Paradox régénérer `/home/yugos06/.local/share/Paradox Interactive/Hearts of Iron IV/mod/The Final Hour.mod`
- Décisions implémentées : D-04, D-05, D-06, FOUND-01
</output>
