---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - common/bookmarks/fh_2026.txt
  - localisation/fh_l_french.yml
  - localisation/fh_l_english.yml
  - .vscode/settings.json
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03

must_haves:
  truths:
    - "Le bookmark fh_2026.txt référence FRA et GER avec les clés de localisation correctes"
    - "GER affiche une description 'En développement' (FH_2026_GER_WIP_DESC) au lieu de FH_2026_GER_DESC (D-08)"
    - "Les fichiers .yml de localisation contiennent FH_2026_GER_WIP_DESC en français et anglais"
    - "Tous les fichiers .yml conservent leur BOM UTF-8 (premier octet ef bb bf)"
    - "VS Code force l'encodage utf8bom pour les fichiers .yml via .vscode/settings.json"
  artifacts:
    - path: "common/bookmarks/fh_2026.txt"
      provides: "Bookmark 2026 avec FRA jouable et GER en WIP"
      contains: "FH_2026_GER_WIP_DESC"
    - path: "localisation/fh_l_french.yml"
      provides: "Clé FH_2026_GER_WIP_DESC en français"
      contains: "FH_2026_GER_WIP_DESC:"
    - path: "localisation/fh_l_english.yml"
      provides: "Clé FH_2026_GER_WIP_DESC en anglais"
      contains: "FH_2026_GER_WIP_DESC:"
    - path: ".vscode/settings.json"
      provides: "Config VS Code forçant utf8bom pour les .yml"
      contains: "utf8bom"
  key_links:
    - from: "common/bookmarks/fh_2026.txt"
      to: "localisation/fh_l_french.yml + fh_l_english.yml"
      via: "Référence à la clé FH_2026_GER_WIP_DESC"
      pattern: 'history = "FH_2026_GER_WIP_DESC"'
---

<objective>
Finaliser le bookmark 2026 (FOUND-02) avec la description WIP pour GER (D-08), maintenir le picture vanilla 1936 (D-09), et configurer VS Code pour garantir l'encodage UTF-8 BOM sur les fichiers de localisation (FOUND-03 + D-10).

Purpose: Permet au joueur de voir et sélectionner le bookmark 2026 avec FRA jouable et GER honnêtement marqué comme en développement. La config VS Code prévient les pertes de BOM lors des éditions futures (sans BOM, HoI4 ignore silencieusement le fichier de localisation entier).

Output: bookmark mis à jour, 2 fichiers .yml enrichis avec la clé GER WIP en FR/EN (avec BOM préservé), config VS Code créée.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@CLAUDE.md
@common/bookmarks/fh_2026.txt
@localisation/fh_l_french.yml
@localisation/fh_l_english.yml

<interfaces>
<!-- État actuel du bookmark — la seule modification est history GER -->
État actuel `common/bookmarks/fh_2026.txt`:
```
bookmarks = {
  bookmark = {
    name = "FH_2026_NAME"
    desc = "FH_2026_DESC"
    date = 2026.1.1.12
    picture = "GFX_select_date_1936"
    default_country = "FRA"
    default = yes

    FRA = {
      history = "FH_2026_FRA_DESC"
      ideology = democratic
    }

    GER = {
      history = "FH_2026_GER_DESC"   <-- DOIT devenir "FH_2026_GER_WIP_DESC"
      ideology = democratic
    }
  }
}
```

Clé localisation à ajouter (français):
```yaml
  FH_2026_GER_WIP_DESC: "L'Allemagne est une grande puissance en profonde mutation. Contenu en développement — disponible dans une prochaine mise à jour."
```

Clé localisation à ajouter (anglais — convention CLAUDE.md : tout en français même dans le fichier anglais MAIS la simultanéité FR/EN imposée par l'init STATE.md prime; on rédige du contenu anglais réel ici puisque c'est une nouvelle clé bilingue dès Phase 1):
```yaml
  FH_2026_GER_WIP_DESC: "Germany is a major power undergoing deep transformation. Content in development — available in a future update."
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Mettre à jour le bookmark fh_2026.txt avec la référence WIP pour GER</name>
  <files>common/bookmarks/fh_2026.txt</files>
  <read_first>
    - common/bookmarks/fh_2026.txt (état actuel — la seule ligne à modifier est history GER)
    - .planning/phases/01-foundation/01-CONTEXT.md §decisions D-08 (GER reste visible avec description "En développement")
    - .planning/phases/01-foundation/01-CONTEXT.md §decisions D-09 (picture vanilla GFX_select_date_1936 conservée)
    - .planning/phases/01-foundation/01-PATTERNS.md (section "common/bookmarks/fh_2026.txt" — modification exacte spécifiée)
  </read_first>
  <action>
    Implémente FOUND-02 + D-08 + D-09. Modification minimale : changer UNIQUEMENT la valeur de `history` du bloc GER de `"FH_2026_GER_DESC"` vers `"FH_2026_GER_WIP_DESC"`. NE PAS toucher à FRA, à picture, à default_country, à date, ni à la structure.

    Le fichier final doit être EXACTEMENT (per D-08 — référence WIP pour GER ; per D-09 — picture 1936 conservée) :

```
bookmarks = {
  bookmark = {
    name = "FH_2026_NAME"
    desc = "FH_2026_DESC"
    date = 2026.1.1.12
    picture = "GFX_select_date_1936"
    default_country = "FRA"
    default = yes

    FRA = {
      history = "FH_2026_FRA_DESC"
      ideology = democratic
    }

    GER = {
      history = "FH_2026_GER_WIP_DESC"
      ideology = democratic
    }
  }
}
```

    Encodage : UTF-8 sans BOM (fichier .txt — voir PATTERNS.md "Encodage des fichiers Clausewitz Script").
  </action>
  <acceptance_criteria>
    - `grep -c 'FH_2026_GER_WIP_DESC' common/bookmarks/fh_2026.txt` retourne `1`
    - `grep -c '"FH_2026_GER_DESC"' common/bookmarks/fh_2026.txt` retourne `0` (l'ancienne clé sans WIP n'apparaît plus)
    - `grep -c 'picture = "GFX_select_date_1936"' common/bookmarks/fh_2026.txt` retourne `1` (D-09 préservée)
    - `grep -c 'default_country = "FRA"' common/bookmarks/fh_2026.txt` retourne `1`
    - `grep -c 'date = 2026.1.1.12' common/bookmarks/fh_2026.txt` retourne `1`
    - `grep -c 'history = "FH_2026_FRA_DESC"' common/bookmarks/fh_2026.txt` retourne `1`
    - `hexdump -C common/bookmarks/fh_2026.txt | head -1` ne commence PAS par `ef bb bf`
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'FH_2026_GER_WIP_DESC' common/bookmarks/fh_2026.txt && [ $(grep -c '"FH_2026_GER_DESC"' common/bookmarks/fh_2026.txt) -eq 0 ] && grep -q 'picture = "GFX_select_date_1936"' common/bookmarks/fh_2026.txt && grep -q 'default_country = "FRA"' common/bookmarks/fh_2026.txt && echo PASS</automated>
  </verify>
  <done>Bookmark fh_2026.txt modifié : GER référence FH_2026_GER_WIP_DESC ; FRA, date, picture, default_country inchangés ; UTF-8 sans BOM.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Ajouter la clé FH_2026_GER_WIP_DESC dans les deux fichiers de localisation (FR + EN)</name>
  <files>localisation/fh_l_french.yml, localisation/fh_l_english.yml</files>
  <read_first>
    - localisation/fh_l_french.yml (vérifier que le BOM est présent et identifier la zone d'insertion logique)
    - localisation/fh_l_english.yml (idem)
    - .planning/phases/01-foundation/01-PATTERNS.md (section "Encodage des fichiers de localisation" — UTF-8 avec BOM obligatoire)
    - .planning/phases/01-foundation/01-RESEARCH.md (section "Anti-patterns à éviter" — sauvegarder .yml sans BOM = HoI4 ignore silencieusement le fichier)
    - CLAUDE.md (convention : tout en français MÊME dans le fichier anglais ; mais init STATE.md décide la simultanéité FR/EN — pour cette nouvelle clé Phase 1, écrire du contenu anglais authentique)
  </read_first>
  <action>
    Implémente la nouvelle clé localisation référencée par le bookmark (Task 1).

    1. Lire le BOM actuel de chaque fichier .yml et le préserver impérativement. Vérifier AVANT édition :
       `hexdump -C localisation/fh_l_french.yml | head -1` doit montrer `ef bb bf 6c 5f 66 72 65` (= BOM + "l_fre").

    2. Ajouter la ligne suivante à `localisation/fh_l_french.yml` (après la ligne `l_french:` dans une zone cohérente — par exemple groupée avec d'autres clés FH_2026_* si elles existent, sinon à la fin du fichier avec un commentaire de section) :
       
```yaml
  FH_2026_GER_WIP_DESC:0 "L'Allemagne est une grande puissance en profonde mutation. Contenu en développement — disponible dans une prochaine mise à jour."
```

       Note : la convention HoI4 utilise `KEY:0 "value"` (le `:0` est le numéro de version de la clé, requis pour HoI4). Vérifier le format des autres clés du fichier — si elles utilisent `KEY: "value"` sans `:0`, suivre le même style. NE PAS mélanger les conventions dans un même fichier.

    3. Ajouter la ligne équivalente à `localisation/fh_l_english.yml` (après la ligne `l_english:`) :

```yaml
  FH_2026_GER_WIP_DESC:0 "Germany is a major power undergoing deep transformation. Content in development — available in a future update."
```

    4. APRÈS édition, vérifier impérativement avec `hexdump -C localisation/fh_l_french.yml | head -1` que la première séquence d'octets est toujours `ef bb bf` (BOM préservé). Si Claude utilise un éditeur qui supprime le BOM, REGÉNÉRER le BOM avec :
       `printf '\xef\xbb\xbf' | cat - localisation/fh_l_french.yml.tmp > localisation/fh_l_french.yml`

    5. Encodage : UTF-8 AVEC BOM (`ef bb bf` en premier — voir PATTERNS.md "Encodage des fichiers de localisation").

    6. Indentation : 2 espaces avant la clé (convention HoI4 confirmée par PATTERNS.md ligne 506).

    7. Ne PAS toucher aux autres clés existantes du fichier (lecture-seule sur le reste du contenu).
  </action>
  <acceptance_criteria>
    - `grep -c 'FH_2026_GER_WIP_DESC' localisation/fh_l_french.yml` retourne `1`
    - `grep -c 'FH_2026_GER_WIP_DESC' localisation/fh_l_english.yml` retourne `1`
    - `hexdump -C localisation/fh_l_french.yml | head -1` commence par `ef bb bf`
    - `hexdump -C localisation/fh_l_english.yml | head -1` commence par `ef bb bf`
    - `head -1 localisation/fh_l_french.yml | tr -d '\357\273\277'` retourne `l_french:` (BOM stripped, header intact)
    - `head -1 localisation/fh_l_english.yml | tr -d '\357\273\277'` retourne `l_english:` (BOM stripped, header intact)
    - `grep 'En développement' localisation/fh_l_french.yml` retourne au moins 1 ligne
    - `grep 'in development' localisation/fh_l_english.yml` retourne au moins 1 ligne
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'FH_2026_GER_WIP_DESC' localisation/fh_l_french.yml && grep -q 'FH_2026_GER_WIP_DESC' localisation/fh_l_english.yml && [ "$(hexdump -C localisation/fh_l_french.yml | head -1 | awk '{print $2$3$4}')" = "efbbbf" ] && [ "$(hexdump -C localisation/fh_l_english.yml | head -1 | awk '{print $2$3$4}')" = "efbbbf" ] && echo PASS</automated>
  </verify>
  <done>Les deux fichiers .yml contiennent la clé FH_2026_GER_WIP_DESC en français et anglais ; BOM ef bb bf préservé sur les deux fichiers ; le contenu existant est intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Créer .vscode/settings.json pour forcer utf8bom sur les .yml</name>
  <files>.vscode/settings.json</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md §decisions D-10 (.vscode/settings.json avec "files.encoding": "utf8bom" pour les .yml)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 6 — config exacte JSON)
    - .planning/phases/01-foundation/01-PATTERNS.md (section "Encodage des fichiers de localisation" — config VS Code recommandée)
  </read_first>
  <action>
    Implémente FOUND-03 (config VS Code utf8bom) et D-10 (clé exacte files.encoding: utf8bom pour [yaml]).

    1. Vérifier que `.vscode/` n'existe pas encore : `ls .vscode/ 2>/dev/null` → si existe, lire le contenu existant pour faire un merge non-destructif. Si n'existe pas, créer le dossier puis le fichier.

    2. Créer `.vscode/settings.json` avec EXACTEMENT ce contenu (per D-10 — utf8bom obligatoire pour les .yml ; per RESEARCH Pattern 6) :

```json
{
  "[yaml]": {
    "files.encoding": "utf8bom"
  },
  "files.associations": {
    "*.yml": "yaml"
  }
}
```

    3. Encodage : UTF-8 sans BOM (fichier .json, pas .yml). LF line endings.

    4. NE PAS ajouter d'autres clés (formatage, linting, etc.) — minimum requis pour FOUND-03.
  </action>
  <acceptance_criteria>
    - `test -f .vscode/settings.json && echo OK` retourne `OK`
    - `grep -c '"files.encoding": "utf8bom"' .vscode/settings.json` retourne `1`
    - `grep -c '"\[yaml\]"' .vscode/settings.json` retourne `1`
    - `grep -c '"\*.yml": "yaml"' .vscode/settings.json` retourne `1`
    - `python3 -c "import json; json.load(open('.vscode/settings.json'))" && echo VALID_JSON` retourne `VALID_JSON` (JSON syntaxiquement valide)
    - `hexdump -C .vscode/settings.json | head -1` ne commence PAS par `ef bb bf` (settings.json sans BOM)
  </acceptance_criteria>
  <verify>
    <automated>test -f .vscode/settings.json && python3 -c "import json; json.load(open('.vscode/settings.json'))" && grep -q '"files.encoding": "utf8bom"' .vscode/settings.json && grep -q '"\[yaml\]"' .vscode/settings.json && echo PASS</automated>
  </verify>
  <done>.vscode/settings.json existe, JSON valide, contient le bloc [yaml] avec files.encoding=utf8bom et l'association *.yml→yaml ; sans BOM sur le .json lui-même.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Système de fichiers local → moteur Paradox | Lecture seule des fichiers script et localisation. Aucune communication réseau. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | N/A | Bookmark / Localisation / Config VS Code | accept | Aucune surface réseau, aucun input utilisateur, aucune authentification. ASVS non applicable per security_enforcement de la phase. |
</threat_model>

<verification>
Après les 3 tâches :
- `grep 'FH_2026_GER_WIP_DESC' common/bookmarks/fh_2026.txt localisation/fh_l_french.yml localisation/fh_l_english.yml | wc -l` → doit retourner `3` (une par fichier)
- `hexdump -C localisation/fh_l_french.yml | head -1` et `hexdump -C localisation/fh_l_english.yml | head -1` → premier triplet `ef bb bf`
- Test manuel HoI4 (utilisateur) : lancer le mod, dans le menu principal → bookmark "2026", France et Allemagne sont visibles ; sélectionner Allemagne → la description affichée correspond au contenu de FH_2026_GER_WIP_DESC.
</verification>

<success_criteria>
1. Bookmark `fh_2026.txt` référence `FH_2026_GER_WIP_DESC` (et plus `FH_2026_GER_DESC`)
2. Les deux fichiers `.yml` contiennent la clé `FH_2026_GER_WIP_DESC` traduite (FR + EN)
3. BOM `ef bb bf` confirmé sur les deux fichiers `.yml` après édition
4. `.vscode/settings.json` existe et déclare `files.encoding=utf8bom` pour les `.yml`
5. Aucun autre fichier n'a été modifié (descriptor.mod, focus tree, etc. inchangés par ce plan)
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-foundation/01-02-SUMMARY.md` documentant :
- Modification précise du bookmark (1 ligne changée — diff avant/après)
- Clés ajoutées dans chaque .yml (2 clés au total : 1 FR, 1 EN)
- BOM vérifié et préservé (sortie hexdump avant/après)
- Création de .vscode/settings.json avec la config minimale
- Décisions implémentées : D-08, D-09, D-10, FOUND-02, FOUND-03
</output>
