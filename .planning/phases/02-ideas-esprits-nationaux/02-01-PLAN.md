---
phase: 02-ideas-esprits-nationaux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - common/ideas/FRA_ideas.txt
autonomous: true
requirements:
  - IDEA-01
  - IDEA-02
  - IDEA-03
  - IDEA-04
  - PARL-01

must_haves:
  truths:
    - "give_idea = FRA_assemblee_nationale via console affiche le malus PP (political_power_gain = -1.0) dans l'UI HoI4"
    - "Les 4 esprits militaires (FRA_armee_de_terre_spirit, FRA_marine_spirit, FRA_armee_air_spirit, FRA_dissuasion_nucleaire_spirit) sont chargés par HoI4 sans erreur de modifier inconnu"
    - "Les 3 esprits économiques (FRA_crise_energetique_spirit, FRA_relance_nucleaire_spirit, FRA_economie_guerre_spirit) sont assignables via give_idea en console"
    - "Aucun ID idea dans le fichier ne commence par fh_ ou fh_fra_ — tous commencent par FRA_"
    - "FRA_esprit_francais est chargé sans pink square (sprite GFX_idea_FRA_esprit_francais résolu)"
  artifacts:
    - path: "common/ideas/FRA_ideas.txt"
      provides: "Bloc ideas = { country = { ... } } avec les 11 esprits nationaux France"
      contains: "FRA_assemblee_nationale"
      min_lines: 80
  key_links:
    - from: "common/ideas/FRA_ideas.txt"
      to: "interface/fh_ideas.gfx"
      via: "champ picture = GFX_idea_FRA_* dans chaque bloc idea"
      pattern: "picture = GFX_idea_FRA_"
    - from: "common/ideas/FRA_ideas.txt"
      to: "gfx/interface/ideas/"
      via: "sprites déclarés dans fh_ideas.gfx pointant vers les .dds"
      pattern: "FRA_.*\\.dds"
---

<objective>
Implémenter les 11 esprits nationaux France dans `common/ideas/FRA_ideas.txt`.

Purpose: Ce fichier est la couche de données centrale de Phase 2. Les phases 3 (on_actions câblage) et 5 (focus effects) dépendent de ces IDs et modifiers. Aucun sprite ni DDS à créer — Phase 1 a tout livré.

Output: `common/ideas/FRA_ideas.txt` contenant les 11 blocs idea complets, syntaxe Paradox valide, prêts pour give_idea en console.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-ideas-esprits-nationaux/02-CONTEXT.md

<interfaces>
<!-- Sprites déjà déclarés dans interface/fh_ideas.gfx — utiliser ces noms EXACTEMENT dans picture = -->
<!-- 11 sprites disponibles : -->
GFX_idea_FRA_esprit_francais
GFX_idea_FRA_assemblee_nationale
GFX_idea_FRA_ve_republique_spirit
GFX_idea_FRA_regime_dexception_spirit
GFX_idea_FRA_armee_de_terre_spirit
GFX_idea_FRA_marine_spirit
GFX_idea_FRA_armee_air_spirit
GFX_idea_FRA_dissuasion_nucleaire_spirit
GFX_idea_FRA_crise_energetique_spirit
GFX_idea_FRA_relance_nucleaire_spirit
GFX_idea_FRA_economie_guerre_spirit

<!-- Fichiers .dds correspondants présents dans gfx/interface/ideas/ — AUCUNE création requise -->

<!-- Squelette actuel de common/ideas/FRA_ideas.txt (Phase 1) : -->
ideas = {
  country = {
    # PHASE 2 : FRA_esprit_francais = { ... }
  }
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : Esprits politiques et esprit de base (4 ideas)</name>
  <files>common/ideas/FRA_ideas.txt</files>

  <read_first>
    - `common/ideas/FRA_ideas.txt` — lire l'état actuel (squelette Phase 1, 9 lignes)
    - `interface/fh_ideas.gfx` — confirmer les noms exact des 11 sprites GFX
    - `.planning/phases/02-ideas-esprits-nationaux/02-CONTEXT.md` — D-10, D-11, D-14
  </read_first>

  <action>
Remplacer le contenu de `common/ideas/FRA_ideas.txt` par le fichier complet incluant les 4 premiers blocs. Le fichier doit rester ouvert pour les tâches suivantes (tâches 2 et 3 ajoutent des blocs dans le même fichier).

Écrire le fichier entier en une seule opération Write (pas d'édition partielle). Structure cible pour cette tâche (bloc ideas ouvert, les 4 premiers ideas, les 7 restants seront ajoutés par les tâches 2 et 3) :

```
# common/ideas/FRA_ideas.txt
# Phase 2 — Esprits nationaux France
# 11 ideas : politique (4) + militaire (4) + économique (3)

ideas = {
  country = {

    # ============================================================
    # ESPRIT DE BASE (permanent, actif dès démarrage — D-02)
    # ============================================================

    FRA_esprit_francais = {
      picture = GFX_idea_FRA_esprit_francais
      allowed = {
        original_tag = FRA
      }
      modifier = {
        research_speed_factor = 0.03
        mobilization_laws_cost_factor = 0.15
      }
    }

    # ============================================================
    # ESPRITS POLITIQUES (IDEA-01)
    # ============================================================

    # Actif dès démarrage (D-01) — coalition instable 577 sièges
    FRA_assemblee_nationale = {
      picture = GFX_idea_FRA_assemblee_nationale
      allowed = {
        original_tag = FRA
      }
      modifier = {
        political_power_gain = -1.0
        stability_factor = -0.05
      }
    }

    # Ajouté par focus FRA_ve_republique_renforcee (D-05, D-14)
    FRA_ve_republique_spirit = {
      picture = GFX_idea_FRA_ve_republique_spirit
      allowed = {
        original_tag = FRA
      }
      modifier = {
        stability_factor = 0.10
        political_power_gain = 0.50
        democratic_drift = 0.05
      }
    }

    # Ajouté par focus FRA_regime_dexception — mutex avec FRA_ve_republique_spirit (D-06, D-14)
    FRA_regime_dexception_spirit = {
      picture = GFX_idea_FRA_regime_dexception_spirit
      allowed = {
        original_tag = FRA
      }
      modifier = {
        political_power_gain = 1.0
        stability_factor = -0.10
        production_speed_buildings_factor = 0.10
        foreign_subversive_activites_cost = 0.25
      }
    }

    # [TÂCHE 2 : esprits militaires — 4 blocs ici]
    # [TÂCHE 3 : esprits économiques — 3 blocs ici]

  }
}
```

Note importante : Écrire le fichier entier (pas uniquement les 4 premiers blocs) en incluant les placeholders commentés pour les tâches 2 et 3. Les tâches 2 et 3 utiliseront Edit pour remplacer les placeholders par le vrai code.

Justification des modifiers (D-14 — modifiers finaux, pas des stubs) :
- `FRA_esprit_francais` : `research_speed_factor = 0.03` (nation des ingénieurs et de la culture scientifique) + `mobilization_laws_cost_factor = 0.15` (vieillissement démographique, conscription coûte plus)
- `FRA_assemblee_nationale` : exactement `political_power_gain = -1.0` + `stability_factor = -0.05` (D-10, D-11, PARL-01)
- `FRA_ve_republique_spirit` : `stability_factor = 0.10` + `political_power_gain = 0.50` + `democratic_drift = 0.05` (Ve République renforcée = démocratie consolidée)
- `FRA_regime_dexception_spirit` : `political_power_gain = 1.0` + `stability_factor = -0.10` + `production_speed_buildings_factor = 0.10` + `foreign_subversive_activites_cost = 0.25` (efficacité exécutive au prix des libertés et isolation diplomatique)

Encodage : UTF-8 sans BOM (fichier .txt, pas .yml).
  </action>

  <verify>
    <automated>grep -c "FRA_assemblee_nationale" /home/yugos06/The-Final-Hour-/common/ideas/FRA_ideas.txt</automated>
    <automated>grep -c "political_power_gain = -1.0" /home/yugos06/The-Final-Hour-/common/ideas/FRA_ideas.txt</automated>
    <automated>grep -c "stability_factor = -0.05" /home/yugos06/The-Final-Hour-/common/ideas/FRA_ideas.txt</automated>
    <automated>grep -c "FRA_ve_republique_spirit" /home/yugos06/The-Final-Hour-/common/ideas/FRA_ideas.txt</automated>
    <automated>grep -c "FRA_regime_dexception_spirit" /home/yugos06/The-Final-Hour-/common/ideas/FRA_ideas.txt</automated>
    <automated>grep -c "FRA_esprit_francais" /home/yugos06/The-Final-Hour-/common/ideas/FRA_ideas.txt</automated>
    <automated>hexdump -n 3 /home/yugos06/The-Final-Hour-/common/ideas/FRA_ideas.txt | head -1</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "FRA_assemblee_nationale" common/ideas/FRA_ideas.txt` retourne >= 1
    - `grep -c "political_power_gain = -1.0" common/ideas/FRA_ideas.txt` retourne >= 1
    - `grep -c "stability_factor = -0.05" common/ideas/FRA_ideas.txt` retourne >= 1
    - `grep -c "FRA_esprit_francais" common/ideas/FRA_ideas.txt` retourne >= 1
    - `hexdump -n 3 common/ideas/FRA_ideas.txt` NE commence PAS par `ef bb bf` (pas de BOM sur fichier .txt)
    - Aucune occurrence de `fh_fra_` dans le fichier : `grep -c "fh_fra_" common/ideas/FRA_ideas.txt` retourne 0
    - Les 4 blocs ideas ont chacun `allowed = { original_tag = FRA }`
  </acceptance_criteria>

  <done>4 esprits (FRA_esprit_francais, FRA_assemblee_nationale, FRA_ve_republique_spirit, FRA_regime_dexception_spirit) présents dans FRA_ideas.txt avec modifiers corrects et syntaxe Paradox valide.</done>
</task>

<task type="auto">
  <name>Tâche 2 : Esprits militaires (4 ideas — état faible de base)</name>
  <files>common/ideas/FRA_ideas.txt</files>

  <read_first>
    - `common/ideas/FRA_ideas.txt` — état après Tâche 1 (contient les 4 premiers esprits et les placeholders commentés)
    - `.planning/phases/02-ideas-esprits-nationaux/02-CONTEXT.md` — D-04, D-09, D-13
  </read_first>

  <action>
Éditer `common/ideas/FRA_ideas.txt` pour remplacer le commentaire `# [TÂCHE 2 : esprits militaires — 4 blocs ici]` par les 4 blocs ideas militaires.

Pattern esprit unique évolutif (D-09) : ces blocs représentent l'état de base (faible). Les focus militaires en Phase 5 utiliseront `remove_ideas` + `add_ideas` pour remplacer ces blocs par des versions renforcées — les IDs restent stables, le contenu évolue via les effect de focus.

Blocs à insérer (remplacer le placeholder commenté) :

```
    # ============================================================
    # ESPRITS MILITAIRES (IDEA-02) — état faible de base (D-04, D-09, D-13)
    # Phase 5 : focus militaires remplaceront ces blocs via remove_ideas + add_ideas
    # ============================================================

    # Armée de terre — fonctionnelle mais sous-financée
    FRA_armee_de_terre_spirit = {
      picture = GFX_idea_FRA_armee_de_terre_spirit
      allowed = {
        original_tag = FRA
      }
      modifier = {
        army_core_attack_factor = -0.05
        army_core_defence_factor = -0.05
        land_reinforce_rate = -0.05
      }
    }

    # Marine nationale — présence opérationnelle limitée
    FRA_marine_spirit = {
      picture = GFX_idea_FRA_marine_spirit
      allowed = {
        original_tag = FRA
      }
      modifier = {
        navy_max_range_factor = -0.05
        ship_repair_speed = -0.05
      }
    }

    # Armée de l'air — capacité opérationnelle réduite
    FRA_armee_air_spirit = {
      picture = GFX_idea_FRA_armee_air_spirit
      allowed = {
        original_tag = FRA
      }
      modifier = {
        air_agility_factor = -0.05
        air_attack_factor = -0.05
      }
    }

    # Dissuasion nucléaire — deterrence maintenue, modernisation en attente
    FRA_dissuasion_nucleaire_spirit = {
      picture = GFX_idea_FRA_dissuasion_nucleaire_spirit
      allowed = {
        original_tag = FRA
      }
      modifier = {
        nuclear_production = -1
        army_core_defence_factor = 0.05
      }
    }
```

Justification des modifiers (D-13 — armée fonctionnelle mais sous-financée, ordre de grandeur -0.05 par branche) :
- Terre : malus attaque/défense -0.05 + renforcement -0.05 (budget limité, équipements vieillissants)
- Marine : portée max -0.05 + vitesse réparation -0.05 (flotte réduite, entretien insuffisant)
- Air : agilité -0.05 + attaque -0.05 (flotte aérienne sous-dimensionnée pour les missions modernes)
- Dissuasion : `nuclear_production = -1` (financement insuffisant pour moderniser) + `army_core_defence_factor = 0.05` (la dissuasion protège quand même — c'est son rôle deterrence)
  </action>

  <verify>
    <automated>grep -c "FRA_armee_de_terre_spirit" /home/yugos06/The-Final-Hour-/common/ideas/FRA_ideas.txt</automated>
    <automated>grep -c "FRA_marine_spirit" /home/yugos06/The-Final-Hour-/common/ideas/FRA_ideas.txt</automated>
    <automated>grep -c "FRA_armee_air_spirit" /home/yugos06/The-Final-Hour-/common/ideas/FRA_ideas.txt</automated>
    <automated>grep -c "FRA_dissuasion_nucleaire_spirit" /home/yugos06/The-Final-Hour-/common/ideas/FRA_ideas.txt</automated>
    <automated>grep -c "army_core_attack_factor = -0.05" /home/yugos06/The-Final-Hour-/common/ideas/FRA_ideas.txt</automated>
  </verify>

  <acceptance_criteria>
    - Chacun des 4 esprits militaires présent au moins une fois dans le fichier
    - Chaque bloc militaire a `allowed = { original_tag = FRA }`
    - Chaque bloc militaire a `picture = GFX_idea_FRA_[nom]` correspondant exactement aux sprites dans fh_ideas.gfx
    - `grep -c "TÂCHE 2" common/ideas/FRA_ideas.txt` retourne 0 (placeholder remplacé)
    - Aucun modifier inconnu HoI4 1.14 : uniquement army_core_attack_factor, army_core_defence_factor, land_reinforce_rate, navy_max_range_factor, ship_repair_speed, air_agility_factor, air_attack_factor, nuclear_production
  </acceptance_criteria>

  <done>4 esprits militaires présents dans FRA_ideas.txt avec modifiers faibles (-0.05) représentant l'état de base 2026, prêts à être renforcés par les effects de focus Phase 5.</done>
</task>

<task type="auto">
  <name>Tâche 3 : Esprits économiques (3 ideas) et validation syntaxe globale</name>
  <files>common/ideas/FRA_ideas.txt</files>

  <read_first>
    - `common/ideas/FRA_ideas.txt` — état après Tâche 2 (8 esprits présents, placeholder Tâche 3 restant)
    - `.planning/phases/02-ideas-esprits-nationaux/02-CONTEXT.md` — D-03, D-07, D-08, D-12
  </read_first>

  <action>
Éditer `common/ideas/FRA_ideas.txt` pour remplacer le commentaire `# [TÂCHE 3 : esprits économiques — 3 blocs ici]` par les 3 blocs ideas économiques.

Blocs à insérer :

```
    # ============================================================
    # ESPRITS ÉCONOMIQUES (IDEA-03)
    # ============================================================

    # Actif dès démarrage (D-03) — crise énergétique 2026, punitif pour forcer action rapide
    FRA_crise_energetique_spirit = {
      picture = GFX_idea_FRA_crise_energetique_spirit
      allowed = {
        original_tag = FRA
      }
      modifier = {
        industrial_capacity_factory = -0.15
        production_speed_buildings_factor = -0.10
      }
    }

    # Ajouté par focus FRA_relance_nucleaire_civil (D-07)
    FRA_relance_nucleaire_spirit = {
      picture = GFX_idea_FRA_relance_nucleaire_spirit
      allowed = {
        original_tag = FRA
      }
      modifier = {
        production_speed_nuclear_reactor_factor = 0.20
        industrial_capacity_factory = 0.10
        research_speed_factor = 0.05
      }
    }

    # Ajouté par focus FRA_economie_de_guerre (D-08)
    FRA_economie_guerre_spirit = {
      picture = GFX_idea_FRA_economie_guerre_spirit
      allowed = {
        original_tag = FRA
      }
      modifier = {
        industrial_capacity_factory = 0.15
        industrial_capacity_dockyard = 0.10
        production_speed_arms_factory_factor = 0.20
        stability_factor = -0.05
      }
    }
```

Justification des modifiers (D-12 pour crise énergétique, cohérence narrative pour les deux autres) :
- `FRA_crise_energetique_spirit` : exactement `industrial_capacity_factory = -0.15` + `production_speed_buildings_factor = -0.10` (D-12 — punitif, force action rapide sur branche économique)
- `FRA_relance_nucleaire_spirit` : bonus prod nucléaire +20% + capacité industrie +10% + recherche +5% (récompense investissement dans l'énergie)
- `FRA_economie_guerre_spirit` : industrie +15% + chantiers +10% + usines d'armement +20% + stabilité -5% (économie de guerre mobilise tout, tensions internes)

Note sur `production_speed_nuclear_reactor_factor` : vérifier la validité exacte dans HoI4 1.14 — si ce modifier n'existe pas, remplacer par `production_speed_industrial_complex_factor = 0.15` (modifier certain HoI4 1.14).

Après insertion des 3 derniers blocs, vérifier la syntaxe globale du fichier :
- Le fichier doit commencer par `ideas = {` et terminer par `}` (fermeture du bloc country + fermeture du bloc ideas)
- Chacun des 11 blocs ideas doit avoir `{` ouvert et `}` fermé
- Aucun placeholder commenté `# [TÂCHE X ...]` ne doit rester dans le fichier final
- Compter les `{` et `}` — ils doivent être équilibrés (même nombre d'ouvertures et fermetures)

Résultat final : fichier complet avec les 11 esprits nationaux France.
  </action>

  <verify>
    <automated>grep -c "FRA_crise_energetique_spirit" /home/yugos06/The-Final-Hour-/common/ideas/FRA_ideas.txt</automated>
    <automated>grep -c "industrial_capacity_factory = -0.15" /home/yugos06/The-Final-Hour-/common/ideas/FRA_ideas.txt</automated>
    <automated>grep -c "FRA_relance_nucleaire_spirit" /home/yugos06/The-Final-Hour-/common/ideas/FRA_ideas.txt</automated>
    <automated>grep -c "FRA_economie_guerre_spirit" /home/yugos06/The-Final-Hour-/common/ideas/FRA_ideas.txt</automated>
    <automated>grep -v "^#" /home/yugos06/The-Final-Hour-/common/ideas/FRA_ideas.txt | grep -c "fh_fra_"</automated>
    <automated>python3 -c "
txt = open('/home/yugos06/The-Final-Hour-/common/ideas/FRA_ideas.txt').read()
opens = txt.count('{')
closes = txt.count('}')
print(f'opens={opens} closes={closes} balanced={opens==closes}')
"</automated>
  </verify>

  <acceptance_criteria>
    - Les 3 esprits économiques présents chacun au moins une fois dans le fichier
    - `grep -c "industrial_capacity_factory = -0.15" common/ideas/FRA_ideas.txt` retourne >= 1 (D-12)
    - `grep -v "^#" common/ideas/FRA_ideas.txt | grep -c "fh_fra_"` retourne 0 (aucun ancien ID non-préfixé FRA_)
    - Accolades équilibrées : même nombre de `{` et `}` dans le fichier
    - Exactement 11 occurrences de `original_tag = FRA` dans le fichier (une par idea)
    - `grep -c "TÂCHE" common/ideas/FRA_ideas.txt` retourne 0 (tous les placeholders remplacés)
    - Le fichier commence par `# common/ideas/FRA_ideas.txt` et contient le bloc racine `ideas = { country = { ... } }`
  </acceptance_criteria>

  <done>FRA_ideas.txt contient les 11 esprits nationaux France complets (4 politiques + 4 militaires + 3 économiques), syntaxe Paradox valide, prêts pour give_idea en console HoI4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fichier mod → Parser HoI4 | Le parser Clausewitz lit FRA_ideas.txt au lancement — une syntaxe invalide crash le chargement du mod |
| Sprite ID → GFX déclaration | Chaque `picture =` doit correspondre exactement à un `name =` dans fh_ideas.gfx — un écart produit pink square |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | FRA_ideas.txt — accolades non équilibrées | mitigate | Vérification python3 count `{` vs `}` dans acceptance_criteria Tâche 3 |
| T-02-02 | Repudiation | IDs ideas sans préfixe FRA_ (anciens fh_fra_) | mitigate | `grep -v "^#" \| grep -c "fh_fra_"` retourne 0 dans chaque tâche |
| T-02-03 | Denial | Modifier inconnu HoI4 1.14 cause erreur dans error.log | mitigate | Modifiers limités aux valeurs documentées HoI4 1.14 dans hoi4_constraints ; note sur production_speed_nuclear_reactor_factor dans Tâche 3 |
| T-02-04 | Elevation | Modifier trop puissant casse l'équilibre gameplay | accept | Valeurs dans fourchette D-13 (~-0.05 militaires), rationale documentée dans chaque action |
</threat_model>

<verification>
## Vérification globale du Plan 01

Après exécution des 3 tâches, vérifier :

```bash
# 1. Compter les 11 esprits (11 occurrences de original_tag = FRA)
grep -c "original_tag = FRA" common/ideas/FRA_ideas.txt
# Attendu : 11

# 2. Aucun ancien ID non-préfixé
grep -v "^#" common/ideas/FRA_ideas.txt | grep -c "fh_fra_"
# Attendu : 0

# 3. PARL-01 — modifiers exacts FRA_assemblee_nationale
grep -A 10 "FRA_assemblee_nationale = {" common/ideas/FRA_ideas.txt
# Attendu : political_power_gain = -1.0 et stability_factor = -0.05 présents

# 4. IDEA-03 — punitif crise énergétique (D-12)
grep -A 8 "FRA_crise_energetique_spirit = {" common/ideas/FRA_ideas.txt
# Attendu : industrial_capacity_factory = -0.15 et production_speed_buildings_factor = -0.10

# 5. Accolades équilibrées (syntaxe Paradox)
python3 -c "
txt = open('common/ideas/FRA_ideas.txt').read()
opens = txt.count('{')
closes = txt.count('}')
print(f'balanced={opens==closes} opens={opens} closes={closes}')
"
# Attendu : balanced=True

# 6. Chaque idea référence un sprite GFX valide
grep "picture = GFX_idea_FRA_" common/ideas/FRA_ideas.txt
# Attendu : 11 lignes, toutes avec GFX_idea_FRA_ suivi d'un ID listé dans fh_ideas.gfx
```
</verification>

<success_criteria>
1. `common/ideas/FRA_ideas.txt` contient exactement 11 blocs idea avec le préfixe `FRA_`
2. `FRA_assemblee_nationale` a `political_power_gain = -1.0` et `stability_factor = -0.05` (PARL-01, D-10, D-11)
3. `FRA_crise_energetique_spirit` a `industrial_capacity_factory = -0.15` et `production_speed_buildings_factor = -0.10` (D-12)
4. Les 4 esprits militaires ont des modifiers dans la fourchette -0.05 (D-13)
5. Aucun ID dans le fichier ne commence par `fh_fra_` ou `fh_` — tous commencent par `FRA_` (IDEA-04)
6. Accolades Paradox équilibrées — le fichier se parse sans erreur de syntaxe
7. Chaque idea a `picture = GFX_idea_FRA_[nom]` correspondant exactement aux sprites déclarés dans `interface/fh_ideas.gfx`
</success_criteria>

<output>
Après completion, créer `.planning/phases/02-ideas-esprits-nationaux/02-01-SUMMARY.md`
</output>
